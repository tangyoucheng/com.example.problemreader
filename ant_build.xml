<?xml version="1.0" encoding="UTF-8"?>
<project name="china-framework" default="build-jar" basedir=".">

    <property name="version" value=""/>
    <property name="jar.name" value="unicorn.framework${version}.jar"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="src.dir" value="src"/>
    <property name="resources.dir" value="embedded"/>

    <!-- JAR 出力先 -->
    <target name="init">
        <mkdir dir="${build.dir}/ant/jar"/>
    </target>

    <!-- source-in-jar -->
    <target name="build-jar" depends="init">
        <jar destfile="${build.dir}/ant/jar/${jar.name}">

            <!-- １、クラスファイル -->
            <fileset dir="${classes.dir}" includes="**/*.class"/>

            <!-- ２、ソースファイル -->
            <fileset dir="${src.dir}" includes="**/*.java"/>

            <!-- ３、embedded リソース -->
            <fileset dir="${resources.dir}" includes="**/*.*"/>

            <!-- ４、ルートディレクトリの指定ファイル -->
            <fileset dir="." includes="mirage-generate-entity.dtd,mirage-generate-entity.xml"/>

        </jar>
    </target>

    <target name="clean">
        <delete dir="${build.dir}/ant/jar"/>
    </target>

</project>
